# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json

name: Build Hub

on:
  push:
    branches:
      - 'main'
    tags:
      - '*'
    paths-ignore:
      - 'website/**'
      - 'docker-autoheal/**'
      - 'sdk/**'
  pull_request:
    paths-ignore:
      - 'website/**'
      - 'docker-autoheal/**'
      - 'sdk/**'

jobs:
  validate-migrations:
    name: Validate migrations
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - run: hack/validate-migrations.sh

  lint-and-test-go:
    name: Lint and test (Go)
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Setup Go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: 'go.mod'
          check-latest: true
          cache-dependency-path: |
            go.sum
      - name: Run go tests
        run: go test ./...
      - name: Lint with golangci-lint
        uses: golangci/golangci-lint-action@ba0d7d2ec06a0ea1cb5fa41b2e4a3ab91d21278a # v9.3.0
        with:
          version: 'v2.13.2' # renovate: datasource=github-releases depName=golangci/golangci-lint

  lint-and-test-node:
    name: Lint and test (Node)
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Install pnpm
        uses: pnpm/action-setup@ea17c68df8912ef543352723c149a84f56e3d413 # v6.1.0
      - name: Setup node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version-file: 'package.json'
          cache: pnpm
          registry-url: 'https://registry.npmjs.org'
      - name: Install frontend dependencies with pnpm
        run: pnpm install --frozen-lockfile
      - name: Lint with prettier
        run: pnpm run lint
      - name: Run tests
        run: pnpm run test

  build:
    services:
      postgres:
        image: postgres
        env:
          POSTGRES_USER: test-user
          POSTGRES_PASSWORD: test-password
          POSTGRES_DB: distr
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432
    name: Build
    timeout-minutes: 10
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
      packages: write
    strategy:
      matrix:
        edition:
          - name: community
            suffix: '-ce'
            images: |
              ghcr.io/distr-sh/distr-ce
          - name: enterprise
            suffix: -ee
            images: |
              ghcr.io/distr-sh/distr-ee
              registry.distr.sh/enterprise/distr-ee
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup Go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: 'go.mod'
          check-latest: true
          cache-dependency-path: |
            go.sum

      - name: Install pnpm
        uses: pnpm/action-setup@ea17c68df8912ef543352723c149a84f56e3d413 # v6.1.0

      - name: Setup node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version-file: 'package.json'
          cache: pnpm
          registry-url: 'https://registry.npmjs.org'

      - name: Generate commit hash
        id: hash
        run: echo "sha_short=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT

      - name: Install frontend dependencies with pnpm
        run: pnpm install --frozen-lockfile

      - name: Build frontend
        if: ${{ !startsWith(github.ref, 'refs/tags/') }}
        run: pnpm run build:${{ matrix.edition.name }} --source-map=true

      - name: Build frontend and upload source maps (tag only)
        if: ${{ startsWith(github.ref, 'refs/tags/') }}
        run: |
          pnpm run build:${{ matrix.edition.name }} --source-map=true || exit 1
          pnpm run sentry-upload || exit 1
          rm internal/frontend/dist/ui/browser/*.map || exit 1
        env:
          SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
          VERSION: ${{ github.ref_name }}
          SENTRY_VERSION: ${{ github.ref_name }}${{ matrix.edition.suffix }}

      - name: Write license public key (tag only)
        if: ${{ matrix.edition.name == 'enterprise' && startsWith(github.ref, 'refs/tags/') }}
        run: echo "${{ secrets.LICENSE_PUBLIC_KEY }}" > internal/license/embedded/pubkey.pem

      - name: Generate ephemeral license key (non-tag)
        id: ephemeral-key
        if: ${{ matrix.edition.name == 'enterprise' && !startsWith(github.ref, 'refs/tags/') }}
        run: |
          openssl genpkey -algorithm ed25519 -out /tmp/ephemeral_private.pem
          openssl pkey -in /tmp/ephemeral_private.pem -pubout -out internal/license/embedded/pubkey.pem
          {
            echo "private-key<<EOF"
            cat /tmp/ephemeral_private.pem
            echo "EOF"
          } >> $GITHUB_OUTPUT

      - name: Build application (amd64)
        run: go build -ldflags="$LDFLAGS" -o dist/distr-amd64 ./cmd/hub/
        env:
          CGO_ENABLED: '0'
          GOOS: linux
          GOARCH: amd64
          LDFLAGS: >-
            -X github.com/distr-sh/distr/internal/buildconfig.version=${{ github.ref_name }}
            -X github.com/distr-sh/distr/internal/buildconfig.commit=${{ steps.hash.outputs.sha_short }}
            -X github.com/distr-sh/distr/internal/buildconfig.edition=${{ matrix.edition.name }}
            ${{ (matrix.edition.name == 'enterprise' && startsWith(github.ref, 'refs/tags/')) && format('-X github.com/distr-sh/distr/internal/license.organizationID={0} -X github.com/distr-sh/distr/internal/license.organizationScopeCutoff={1}', secrets.LICENSE_ORGANIZATION_ID, secrets.LICENSE_ORGANIZATION_SCOPE_CUTOFF) || '' }}

      - name: Build application (arm64)
        if: ${{ startsWith(github.ref, 'refs/tags/') }}
        run: go build -ldflags="$LDFLAGS" -o dist/distr-arm64 ./cmd/hub/
        env:
          CGO_ENABLED: '0'
          GOOS: linux
          GOARCH: arm64
          LDFLAGS: >-
            -X github.com/distr-sh/distr/internal/buildconfig.version=${{ github.ref_name }}
            -X github.com/distr-sh/distr/internal/buildconfig.commit=${{ steps.hash.outputs.sha_short }}
            -X github.com/distr-sh/distr/internal/buildconfig.edition=${{ matrix.edition.name }}
            ${{ matrix.edition.name == 'enterprise' && format('-X github.com/distr-sh/distr/internal/license.organizationID={0} -X github.com/distr-sh/distr/internal/license.organizationScopeCutoff={1}', secrets.LICENSE_ORGANIZATION_ID, secrets.LICENSE_ORGANIZATION_SCOPE_CUTOFF) || '' }}

      - name: Generate ephemeral license JWT (non-tag enterprise only)
        id: ephemeral-license
        if: ${{ matrix.edition.name == 'enterprise' && !startsWith(github.ref, 'refs/tags/') }}
        run: echo "license-key=$(dist/distr-amd64 license-key generate --no-save --payload='{"ld":{}}')" >> $GITHUB_OUTPUT
        env:
          LICENSE_KEY_PRIVATE_KEY: ${{ steps.ephemeral-key.outputs.private-key }}
          DATABASE_URL: postgres://placeholder
          JWT_SECRET: AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=
          DISTR_HOST: http://placeholder
          LOKI_URL: http://placeholder

      - name: Start Hub and verify migrations and docs
        shell: bash
        run: |
          dist/distr-amd64 serve &
          curl --retry 10 --retry-delay 1 --retry-connrefused -sf "$DISTR_HOST/ready" || { echo "Hub did not become ready in time"; exit 1; }
          curl $DISTR_HOST/docs/openapi.json -sf -o /dev/null || { echo "OpenAPI spec is not valid or not reachable at $DISTR_HOST/docs/openapi.json"; exit 1; }
          psql $DATABASE_URL -c "select version > 0, dirty from schema_migrations" --csv -t | grep "t,f"
        env:
          DATABASE_URL: postgres://test-user:test-password@localhost:5432/distr
          JWT_SECRET: H4V7z6mEpe8/k5H/KyogT/iATJhNpEIUMd5cHF6mqF8=
          DISTR_HOST: http://localhost:8080
          LOKI_URL: http://localhost:3100
          USER_EMAIL_VERIFICATION_REQUIRED: 'false'
          LICENSE_KEY: ${{ matrix.edition.name == 'enterprise' && (startsWith(github.ref, 'refs/tags/') && secrets.LICENSE_KEY || steps.ephemeral-license.outputs.license-key) || '' }}

      - name: Run e2e smoke test
        shell: bash
        run: |
          hack/e2e-smoke-test.mjs
          pkill -SIGTERM distr-amd64
          dist/distr-amd64 migrate --down
        env:
          DATABASE_URL: postgres://test-user:test-password@localhost:5432/distr
          JWT_SECRET: H4V7z6mEpe8/k5H/KyogT/iATJhNpEIUMd5cHF6mqF8=
          DISTR_HOST: http://localhost:8080
          LOKI_URL: http://localhost:3100
          USER_EMAIL_VERIFICATION_REQUIRED: 'false'
          LICENSE_KEY: ${{ matrix.edition.name == 'enterprise' && (startsWith(github.ref, 'refs/tags/') && secrets.LICENSE_KEY || steps.ephemeral-license.outputs.license-key) || '' }}

      - name: Generate SBOM for frontend project
        uses: anchore/sbom-action@3ad7283483fc7af8ff2b4ea19663c2d5ca935e26 # v0.24.2
        with:
          file: pnpm-lock.yaml
          output-file: dist/frontend.spdx.json
          artifact-name: frontend${{ matrix.edition.suffix }}.spdx.json
          format: spdx-json
          upload-release-assets: false

      - name: Login to GitHub Container Registry (tag only)
        if: ${{ startsWith(github.ref, 'refs/tags/') }}
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4.6.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Login to Distr Registry (tag only)
        if: ${{ startsWith(github.ref, 'refs/tags/') }}
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4.6.0
        with:
          registry: registry.distr.sh
          username: ${{ github.actor }}
          password: ${{ secrets.DISTR_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@37fe631027851001ddb9b187196cc803df7f5f0e # v4.3.0

      - name: Generate Docker Metadata
        id: meta
        uses: docker/metadata-action@dc802804100637a589fabce1cb79ff13a1411302 # v6.2.0
        with:
          images: ${{ matrix.edition.images }}
          tags: |
            type=ref,event=branch
            type=semver,pattern={{version}}
          flavor: |
            latest=false
          labels: |
            org.opencontainers.image.description=Distr Hub
            org.opencontainers.image.vendor=Glasskube

      - name: Docker build (push on tag only)
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7.3.0
        id: build-push
        with:
          platforms: ${{ startsWith(github.ref, 'refs/tags/') && 'linux/amd64,linux/arm64' || 'linux/amd64' }}
          context: .
          file: Dockerfile.hub
          push: ${{ startsWith(github.ref, 'refs/tags/') }}
          load: ${{ matrix.edition.name == 'community' && !startsWith(github.ref, 'refs/tags/') }}
          sbom: ${{ startsWith(github.ref, 'refs/tags/') }}
          provenance: ${{ startsWith(github.ref, 'refs/tags/') && 'mode=max' || 'false' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Save Docker image for chart test
        if: ${{ matrix.edition.name == 'community' && !startsWith(github.ref, 'refs/tags/') }}
        run: |
          docker tag "${{ steps.build-push.outputs.imageid }}" ghcr.io/distr-sh/distr-ce:ci
          docker save ghcr.io/distr-sh/distr-ce:ci | gzip > /tmp/distr-ce.tar.gz

      - name: Upload Docker image artifact
        if: ${{ matrix.edition.name == 'community' && !startsWith(github.ref, 'refs/tags/') }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: distr-ce-image
          path: /tmp/distr-ce.tar.gz
          retention-days: 1

      - uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6 # v4.1.2
        if: ${{ startsWith(github.ref, 'refs/tags/') }}

      - name: Sign the images with GitHub OIDC Token
        if: ${{ startsWith(github.ref, 'refs/tags/') }}
        env:
          DIGEST: ${{ steps.build-push.outputs.digest }}
          TAGS: ${{ steps.meta.outputs.tags }}
        run: |
          images=""
          for tag in ${TAGS}; do
            images+="${tag}@${DIGEST} "
          done
          cosign sign --yes ${images}

  test-chart-install:
    name: Test Helm chart install
    needs: build
    if: ${{ !startsWith(github.ref, 'refs/tags/') }}
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: deploy/charts/distr
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Download Docker image artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: distr-ce-image
          path: /tmp

      - name: Load Docker image
        run: docker load < /tmp/distr-ce.tar.gz

      - name: Create kind cluster
        uses: helm/kind-action@06c1ae10762d3b9c1644e7fe69596ae519e015a2 # v1.15.0

      - name: Load image into kind
        run: kind load docker-image ghcr.io/distr-sh/distr-ce:ci --name chart-testing

      - name: Build Helm chart dependencies
        run: |
          helm repo add rustfs https://charts.rustfs.com/
          helm dependency build .

      - name: Install Helm chart
        run: |
          helm upgrade --install distr . \
            --set replicaCount=1 \
            --set postgresql.enabled=true \
            --set rustfs.enabled=true \
            --set image.tag=ci \
            --wait --timeout 5m

      - name: Verify /ready endpoint
        run: |
          kubectl port-forward svc/distr 8080:8080 &
          sleep 2
          curl -sf http://localhost:8080/ready
